#Write a method COUNTLINES() in Python to read lines from text file
#‘TESTFILE.TXT’ and display the lines which are not starting with any vowel.
'''
def countlines():
    c=0
    f=open("C:\\Users\\Paarth\\Desktop\\TESTFILE.txt","r")
    d=f.readlines()
    for i in d:
            if i[0] not in ("A","E","I","O","U") and ("a","e","i","o","u"):
                c+=1
                print("The Number of lines not starting with vowels are:",c)    
countlines()
'''
#Write a function ETCount() in Python, which should read each
#character of a text file “TESTFILE.TXT” and then count and display
#the count of occurrence of alphabets E and T individually (including small cases e and t too).  
'''
def ETCount():
    ce=0
    ct=0
    f=open("C:\\Users\\Paarth\\Desktop\\TESTFILE.txt","r")
    d=f.read()
    for i in d:
        if i.lower()=="e":
            ce+=1
        if i.lower()=="t":
            ct+=1
    print("The Number of times E or e occurs is:",ce)
    print("The Number of times T or t occurs is:",ct)
ETCount()
'''
#STACK:-
#Q1) Write a menu driven python program to perform Push, Pop and Display operations for
#stack using list as per the following structure.
stack=[]
while True:
    print("\nSelect Your Choice:")
    print("1.PUSH element into the stack")
    print("2.POP element from the stack")
    print("3.DISPLAY elements of the stack")
    print("4.EXIT")
    choice=int(input("Enter Your Choice:"))
    if choice==1:
        Memberno=int(input("Enter Member Number:"))
        MemberName=str(input("Enter Member Name:"))
        Age=int(input("Enter Member Age:"))
        stack.append([Memberno,MemberName,Age])
    elif choice==2:
        if len(stack)==0:
            print("UNDERFLOW")
        else:
            print("\nElement Poped out from the STACK is:")
            print(stack.pop())
    elif choice==3:
        print("The STACK Elements are:",stack)
    elif choice==4:
        break

    